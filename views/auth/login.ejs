
  <!-- Main Container -->
  <div class="w-full max-w-md mx-auto">
    
    <!-- Header Container (Hijau) -->
    <div class="bg-green-600/90 backdrop-blur-md p-8 rounded-t-2xl shadow-lg text-center">
      <div class="text-5xl text-white mb-2">
        <i class="fas fa-user-circle"></i>
      </div>
      <h1 class="text-2xl font-bold text-white">Selamat Datang</h1>
      <p class="text-sm text-green-100">Masuk ke akun Anda</p>
    </div>

    <!-- Form Container (Transparan) -->
    <div class="bg-white/20 backdrop-blur-md p-8 rounded-b-2xl shadow-lg">
      
      <!-- Login Form -->
      <form action="/auth/login" method="POST" class="space-y-4">
        <!-- Email/NIM -->
        <div class="flex items-center bg-white/80 backdrop-blur-sm rounded-xl px-4 py-3 border border-white/40 hover:bg-white/40 transition-all duration-300">
          <i class="fas fa-envelope text-gray-600 mr-3"></i>
          <input name="identifier" type="text" placeholder="Email atau NIM" required
            class="bg-transparent w-[300px] outline-none text-sm text-gray-800 placeholder-gray-600 border border-transparent focus:border-black p-1 rounded-xl focus:ring-0 " />
        </div>

        <!-- Password -->
        <div class="relative flex items-center bg-white/30 backdrop-blur-sm rounded-xl px-4 py-3 border border-white/40 hover:bg-white/40 transition-all duration-300">
          <i class="fas fa-lock text-gray-600 mr-3"></i>
          <input name="password" type="password" id="password" placeholder="Password" required
            class="bg-transparent w-[300px] outline-none text-sm text-gray-800 placeholder-gray-600 border border-transparent focus:border-black p-1 rounded-xl focus:ring-0" />
          <button type="button" onclick="togglePassword()" class="absolute right-3 text-gray-500">
            <i class="fas fa-eye" id="toggleIcon"></i>
          </button>
        </div>

        <!-- Forgot Password -->
        <div class="text-right text-sm">
          <a href="#" class="text-green-700 hover:text-green-800 hover:underline font-medium transition-colors">Lupa password?</a>
        </div>

        <!-- Submit Button -->
        <button type="submit"
          class="w-full bg-green-600 hover:bg-green-700 text-white py-3 rounded-xl font-semibold transition-all duration-300 flex items-center justify-center gap-2 shadow-lg hover:shadow-xl transform hover:-translate-y-0.5">
          <i class="fas fa-sign-in-alt"></i> Masuk
        </button>
      </form>

      <!-- Register Link -->
      <div class="mt-6 text-sm text-center">
        <span class="text-gray-700">Belum punya akun?</span>
        <a href="/auth/register" class="text-green-700 hover:text-green-800 hover:underline font-medium ml-1 transition-colors">Daftar sekarang</a>
      </div>
    </div>
  </div>

  <!-- Footer -->
  <footer class="mt-8 text-sm text-white/90 text-center">
    © 2024 Your Company. All rights reserved.
  </footer>