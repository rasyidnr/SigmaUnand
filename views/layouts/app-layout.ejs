<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Sigma Unand</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="/css/tailwind.css" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    
    <style>
      body.app-background {
        background-image: url('/images/unand.jpg'); /* Mengambil gambar dari folder public */
        background-size: cover; /* Membuat gambar menutupi seluruh area */
        background-position: center; /* Posisi gambar di tengah */
        background-attachment: fixed; /* Gambar tidak ikut scroll, memberi efek bagus */
      }
    </style>
</head>
<body class="app-background font-sans">
    
    <header class="bg-[#8DAA6F] p-3 px-4 sm:px-6 lg:px-8 shadow-md">
      <div class="grid grid-cols-3 items-center">
        <div>
          <button class="text-white text-2xl"><i class="fas fa-bars"></i></button>
        </div>
        <div class="relative w-full max-w-lg justify-self-center">
          <input type="text" placeholder="Temukan sesuatu" class="w-full py-2 px-4 rounded-full bg-white text-gray-800 focus:outline-none">
          <button class="absolute right-3 top-1/2 -translate-y-1/2 text-gray-500"><i class="fas fa-search"></i></button>
        </div>
        <div class="flex justify-end">
            <a href="/auth/profil-super-admin" class="w-10 h-10 bg-white/30 rounded-full flex items-center justify-center text-white">
                <i class="fas fa-user"></i>
            </a>
        </div>
      </div>
    </header>
    
    <%- body %>
    
    <script src="/javascripts/main.js"></script>
</body>
</html>